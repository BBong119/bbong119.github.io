assets_path: /assets/
theme: jekyll-theme-slateds

collections:
  includes:
    output: true

defaults:
  - scope:
      path: ""
    values:
      layout: "default"
      category: "original"
  - scope:
      path: "testurl"
    values:
      layout: "test"
      category: "test"