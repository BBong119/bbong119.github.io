assets_path: /assets/
theme: jekyll-theme-slateds

defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "default"
      repo: "original-repo"
  - scope:
      path: "testurl/"
    values:
      layout: "default"
      repo: "js-repo"